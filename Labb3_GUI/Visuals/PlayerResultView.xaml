<UserControl x:Class="Labb3_GUI.Visuals.PlayerResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_GUI.Visuals"
             mc:Ignorable="d" 
             Background="{StaticResource BackgroundBrush}"
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" >
            <TextBlock Text="Result" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="40"/>
            <TextBlock Text="{Binding ScoreText}" 
                   FontSize="20" HorizontalAlignment="Center"/>
            <Button Content="Restart Quiz" FontSize="16" Margin="20"
                Command="{Binding RestartQuizCommand}"/>
        </StackPanel>
    
        
    </Grid>
  </UserControl>
