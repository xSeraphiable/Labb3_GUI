<UserControl x:Class="Labb3_GUI.Visuals.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_GUI.Visuals"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            
        <Label Content="Quiz" FontSize="24" Margin="10" HorizontalAlignment="Center"/>
        <TextBlock Text="x / x questions answered" Margin="5" HorizontalAlignment="Center"/>
            
        </StackPanel>
        
        <TextBlock Grid.Row="1" Margin="20" HorizontalAlignment="Center" Text="{Binding ActivePack.Questions[0].Query}"/>
        <TextBlock Grid.Row="1" Margin="20" HorizontalAlignment="Right" Text="{Binding ActivePack.TimeLimitInSeconds}"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button HorizontalAlignment="Center" 
                    VerticalAlignment="Bottom"
                    Margin="25"
                    Height="75"
                    Width="100"
                    Content="{Binding ActivePack.Questions[0].CorrectAnswer}"
                    />
            <Button HorizontalAlignment="Center" 
                    VerticalAlignment="Bottom"
                    Margin="25"
                    Height="75"
                    Width="100"
                    Content="Svar 1"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button HorizontalAlignment="Center" 
            VerticalAlignment="Top"
            Margin="25"
            Height="75"
            Width="100"
            Content="Svar 1"/>
            <Button HorizontalAlignment="Center" 
            VerticalAlignment="Top"
            Margin="25"
            Height="75"
            Width="100"
            Content="Svar 1"/>
        </StackPanel>
        
    </Grid>
</UserControl>
