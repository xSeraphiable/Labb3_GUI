<UserControl x:Class="Labb3_GUI.Visuals.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_GUI.Visuals"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding PlayerViewModel}"
             Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="Fråga" Margin="10" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="0" Margin="20" HorizontalAlignment="Center" Text="{Binding ActivePack.Name}"/>
        <StackPanel Grid.Row="1">
            <Button HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Margin="25"
                    Background="Gainsboro"
                    Content="Svar 1"/>



        </StackPanel>
    </Grid>
</UserControl>
