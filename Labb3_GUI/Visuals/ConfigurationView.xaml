<UserControl x:Class="Labb3_GUI.Visuals.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_GUI.Visuals"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding ConfigurationViewModel}"
             >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0" Grid.Row="0">
            <Label Margin="20,30,20,0" Padding="0" Content="{Binding ActivePack.Name}"/>
        </StackPanel>
        <ListBox Grid.Row="1" x:Name="textblock" Margin="10" 
                   VerticalAlignment="Stretch" ItemsSource="{Binding ActivePack.Questions}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="Question 1" FontStyle="Italic"/>
                        <StackPanel>
                            <TextBlock Text="{Binding Query}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate> 
            </ListBox.ItemTemplate>
        </ListBox>


        <StackPanel Grid.Column="1" Grid.Row="1">
            <Label Margin="10" Padding="0" Content="Question:"/>
            <TextBox Margin="20,0,20,20" Height="100"/>

            <Label Margin="20,10,20,0" Padding="0" Content="Correct answer:"/>
            <TextBox Margin="20,0,20,0"/>
            <Label Margin="20,10,20,0" Padding="0" Content="First incorrect answer:"/>
            <TextBox Margin="20,0,20,0"/>
            <Label Margin="20,10,20,0" Padding="0" Content="Second incorrect answer:"/>
            <TextBox Margin="20,0,20,0"/>
            <Label Margin="20,10,20,0" Padding="0" Content="Third incorrect answer:"/>
            <TextBox Margin="20,0,20,0"/>
            <Button Margin="20" Width="80" Height="20" Content="Save" Click="Button_Click"/>

        </StackPanel>
    </Grid>
</UserControl>
